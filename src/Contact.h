#pragma once
#include <string>

namespace Indra
{

struct Contact
{
    std::string name;    // Display label on the button
    std::string station; // VACS callsign e.g. "EGLL_TWR"
};

} // namespace Indra
